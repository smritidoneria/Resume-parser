type Resume
  @model
  @auth(rules: [{ allow: public, provider: apiKey }]) {
  resumeId: ID!
  name: String
  phone: String
  email: String
  education: String
  experience: String
  skills: String
  rawText: String
}
